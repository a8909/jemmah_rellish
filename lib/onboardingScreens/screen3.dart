import 'package:flutter/material.dart';

class Onbooarding3 extends StatelessWidget {
  const Onbooarding3({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: const Text('screen3'),
    );
  }
}
